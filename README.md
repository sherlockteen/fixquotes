# fixquotes
JSON requires all strings to be denoted in double quotes, while JavaScript permits single quotes for strings. This can lead to inconsistencies where JQ will refuse to parse data but we can fix this with a simple script.
